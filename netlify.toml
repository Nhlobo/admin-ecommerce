[build]
  command = "npm install"
  publish = "."
  functions = "netlify/functions"

[build.environment]
  NODE_ENV = "production"

[[redirects]]
  from = "/*"
  to = "/server.js"
  status = 200

[dev]
  command = "npm start"
  port = 3000
